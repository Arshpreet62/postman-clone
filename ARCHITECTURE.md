# 🏗️ Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT (Vercel)                          │
│                                                                   │
│  ┌────────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │   Components   │  │   Context    │  │   API Service    │    │
│  │                │  │              │  │                  │    │
│  │  - Dashboard   │→ │  - User      │→ │  - Timeout       │    │
│  │  - Form        │  │  - Token     │  │  - Auth Headers  │    │
│  │  - History     │  │  - Response  │  │  - Error Handle  │    │
│  │  - Toast       │  │              │  │                  │    │
│  └────────────────┘  └──────────────┘  └──────────────────┘    │
│                                                ↓                  │
└────────────────────────────────────────────────┼─────────────────┘
                                                  │
                                        HTTPS + CORS
                                                  │
┌─────────────────────────────────────────────────┼─────────────────┐
│                         SERVER (Render)         ↓                 │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Express Server                          │   │
│  │                                                             │   │
│  │  Middleware Stack:                                          │   │
│  │  1. Helmet (Security Headers)                              │   │
│  │  2. CORS (Cross-Origin)                                    │   │
│  │  3. Compression (Gzip)                                     │   │
│  │  4. Rate Limit (100 req/15min)                             │   │
│  │  5. Morgan (Logging)                                       │   │
│  │  6. Body Parser (JSON)                                     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐      │
│  │   Routes     │  │  Middleware  │  │   Services       │      │
│  │              │  │              │  │                  │      │
│  │  /health     │  │  - Auth JWT  │  │  - Logger        │      │
│  │  /api/auth   │→ │  - Validate  │→ │  - Error Handler │      │
│  │  /api/request│  │  - Error     │  │  - Async Wrapper │      │
│  │  /api/history│  │              │  │                  │      │
│  │  /api/stats  │  │              │  │                  │      │
│  └──────────────┘  └──────────────┘  └──────────────────┘      │
│                                                ↓                  │
└────────────────────────────────────────────────┼─────────────────┘
                                                  │
                                        MongoDB Protocol
                                                  │
┌─────────────────────────────────────────────────┼─────────────────┐
│                    DATABASE (MongoDB Atlas)     ↓                 │
│                                                                   │
│  ┌──────────────────┐          ┌──────────────────────────┐     │
│  │     Users        │          │    RequestHistory        │     │
│  ├──────────────────┤          ├──────────────────────────┤     │
│  │ _id              │          │ _id                      │     │
│  │ email (unique)   │          │ user (ref: Users)        │     │
│  │ password (hash)  │          │ endpoint                 │     │
│  │ createdAt        │          │ method                   │     │
│  │ updatedAt        │          │ timestamp (indexed)      │     │
│  └──────────────────┘          │ request { headers, body }│     │
│                                 │ response { status, body }│     │
│                                 └──────────────────────────┘     │
└───────────────────────────────────────────────────────────────────┘
```

## Request Flow

### 1. Authentication Flow

```
User → Login Form → API Service → POST /api/auth/login
                                          ↓
                                   Validate (Zod)
                                          ↓
                                   Find User (MongoDB)
                                          ↓
                                   Compare Password (Bcrypt)
                                          ↓
                                   Generate JWT (7 days)
                                          ↓
                                   Return { token, user }
                                          ↓
Store in localStorage ← API Service ← Response
```

### 2. API Request Flow

```
User → Request Form → API Service → POST /api/request
                                          ↓
                                   Rate Limit Check
                                          ↓
                                   Validate Schema (Zod)
                                          ↓
                                   Optional Auth (JWT)
                                          ↓
                                   Make External Request (30s timeout)
                                          ↓
                                   Save to History (if authenticated)
                                          ↓
                                   Return Response + Duration
                                          ↓
Display Result + Toast ← API Service ← Response
```

### 3. Error Flow

```
Error Occurs → Async Handler → Error Middleware
                                     ↓
                            Check if Operational
                                     ↓
                    ┌────────────────┴────────────────┐
                    │                                 │
              Operational                      Programmer
                    │                                 │
              Log (info)                        Log (error)
                    │                                 │
              User Message                   Generic Message
                    │                                 │
              Status Code                       500 Error
                    ↓                                 ↓
              Response JSON ←───────────────────────┘
```

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                      Security Stack                          │
├─────────────────────────────────────────────────────────────┤
│  Layer 1: Network                                            │
│  - HTTPS (TLS 1.3)                                          │
│  - CORS with specific origins                               │
│  - Rate limiting (IP-based)                                 │
├─────────────────────────────────────────────────────────────┤
│  Layer 2: Headers                                            │
│  - Helmet (11+ security headers)                            │
│  - X-Content-Type-Options: nosniff                          │
│  - X-Frame-Options: SAMEORIGIN                              │
│  - X-XSS-Protection                                         │
├─────────────────────────────────────────────────────────────┤
│  Layer 3: Authentication                                     │
│  - JWT (7-day expiration)                                   │
│  - Bcrypt (12 salt rounds)                                  │
│  - Secure token storage                                     │
├─────────────────────────────────────────────────────────────┤
│  Layer 4: Input Validation                                   │
│  - Zod schemas (type-safe)                                  │
│  - MongoDB query sanitization                               │
│  - JSON size limits (10MB)                                  │
├─────────────────────────────────────────────────────────────┤
│  Layer 5: Error Handling                                     │
│  - No sensitive data in errors                              │
│  - Different messages for dev/prod                          │
│  - Structured logging                                       │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

### Making an API Request

```
1. User fills form
   └─ URL, Method, Headers, Body

2. Frontend validation
   └─ URL format check
   └─ Required fields

3. API Service
   └─ Add auth headers
   └─ Set timeout (30s)
   └─ POST to backend

4. Backend middleware
   └─ Rate limit check
   └─ CORS verification
   └─ Body parsing
   └─ Schema validation (Zod)

5. Route handler
   └─ Optional JWT verification
   └─ Make external request
   └─ Save to MongoDB (if authenticated)
   └─ Return response + metadata

6. Frontend display
   └─ Show response
   └─ Toast notification
   └─ Update history
```

## Component Hierarchy

```
App
├── ToastProvider (global notifications)
│   └── ContextProvider (global state)
│       ├── Route "/"
│       │   └── Landing
│       │       ├── Signup Link
│       │       └── Login Link
│       │
│       ├── Route "/signup"
│       │   └── Signup
│       │       └── Toast notifications
│       │
│       ├── Route "/login"
│       │   └── Login
│       │       └── Toast notifications
│       │
│       └── Route "/dashboard"
│           └── Dashboard
│               ├── Header (user info, logout)
│               ├── Tabs (request, history, stats)
│               ├── Request Tab
│               │   ├── Form (API request)
│               │   └── ResponseDisplay
│               ├── History Tab
│               │   └── RequestHistory (pagination)
│               └── Stats Tab
│                   └── Statistics (charts)
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                         Internet                              │
└──────────────────────────────────────────────────────────────┘
                    │                        │
                    │                        │
        ┌───────────▼────────┐   ┌──────────▼──────────┐
        │   Vercel CDN       │   │   Render Platform   │
        │   (Frontend)       │   │   (Backend)         │
        ├────────────────────┤   ├─────────────────────┤
        │ - React App        │   │ - Node.js Server    │
        │ - Static Files     │   │ - Health Checks     │
        │ - Auto HTTPS       │   │ - Auto Scaling      │
        │ - Edge Network     │   │ - Auto HTTPS        │
        └────────────────────┘   └─────────────────────┘
                                           │
                                           │
                                 ┌─────────▼──────────┐
                                 │  MongoDB Atlas     │
                                 ├────────────────────┤
                                 │ - Managed DB       │
                                 │ - Auto Backups     │
                                 │ - Replication      │
                                 │ - Monitoring       │
                                 └────────────────────┘
```

## Performance Optimization

```
┌─────────────────────────────────────────────────────────────┐
│                    Optimization Stack                        │
├─────────────────────────────────────────────────────────────┤
│  Frontend                                                    │
│  - Code splitting (Vite)                                    │
│  - Asset optimization (minify, compress)                    │
│  - Lazy loading (future)                                    │
│  - CDN delivery (Vercel Edge)                               │
├─────────────────────────────────────────────────────────────┤
│  Backend                                                     │
│  - Compression middleware (70-90% reduction)                │
│  - Database indexing (user, timestamp)                      │
│  - Pagination (limit 100)                                   │
│  - Request timeout (30s)                                    │
│  - Connection pooling (MongoDB)                             │
├─────────────────────────────────────────────────────────────┤
│  Database                                                    │
│  - Compound indexes                                         │
│  - Lean queries (MongoDB)                                   │
│  - Parallel queries (Promise.all)                           │
│  - Connection retry logic                                   │
└─────────────────────────────────────────────────────────────┘
```

## Monitoring Points

```
1. Health Check (/health)
   └─ Uptime monitoring
   └─ Response time
   └─ Server status

2. Logs (Winston)
   └─ Request/response logging
   └─ Error tracking
   └─ Authentication events

3. Metrics (Future)
   └─ Request count
   └─ Error rate
   └─ Response time avg
   └─ Database query time

4. Alerts (Future)
   └─ High error rate
   └─ Slow responses
   └─ Service down
```

## Scalability

```
Current: Single Instance
┌──────────────┐
│   Server 1   │ ← All traffic
└──────────────┘

Future: Horizontal Scaling
┌──────────────┐
│ Load Balancer│
└──────┬───────┘
       │
   ┌───┴───┬───────┬────────┐
   │       │       │        │
┌──▼───┐ ┌─▼───┐ ┌─▼────┐ ┌▼─────┐
│Server│ │Server│ │Server│ │Server│
│  1   │ │  2   │ │  3   │ │  4   │
└──────┘ └─────┘ └──────┘ └──────┘
```

---

**This architecture is production-ready and can scale to thousands of users!**
